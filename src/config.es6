export default {
	"port": 1200,
	"timeout": 560000,
	"console": "default",
	"autoresponder": [
	],
	"httpsHandle":[

		// Significa que todos los requerimientos HTTPS
		// son procesados por este programa, lo cual hace disponible 
		// los autoresponders para HTTPS 
		"all"

	]
}